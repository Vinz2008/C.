/*#ifdef PROFILING
#include "external/tracy/public/tracy/Tracy.hpp"
#else 
#define ZoneScopedN(name) 
#endif*/ // TODO ? (if activated it gives the zones, but you can't see all the functions easily at the same time)

#define ZoneScopedN(name) 