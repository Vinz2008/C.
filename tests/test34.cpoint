import @std/linked_list.cpoint
import @std/print.cpoint

func main(){
    var ll : struct linked_list 
    printd(2)
    for i : i32 = 1, i < 10, 1 { 
        ll.append(i)
    }
    ll.print()
    ll.remove_first()
    ll.remove_last()
    ll.print()
}