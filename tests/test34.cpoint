import @std/linked_list.cpoint
import @std/print.cpoint

func main(){
    var ll : struct linked_list 
    printd(2)
    ll.append(1)
    ll.append(2)
    ll.append(3)
    ll.remove_last()
    ll.print()
}