import @std/print.cpoint

struct a {
    var d : double
    var i : i32
}

func main() void {
    var d : double = 0
    var i : i32 = 0
    var eightb : i8 = 0
    var structa : struct a
    var t1 = typeId d
    var t2 = typeId i
    printd(reflection.typeid(eightb))
    printd(reflection.getmembernb(structa))
    printstr(reflection.getstructname(structa))
}