import @std/print.cpoint
include @std/libc.hpoint
import @std/system.cpoint

func main(){
    var c : i8 = 'b'
    printd(3.2)
    printd(O_WRONLY)
    system::open_url("https://github.com/Vinz2008/C.")
?[if OS == linux]
    printi(system::get_processor_nb())
?[endif]
}
