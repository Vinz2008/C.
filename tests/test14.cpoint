import @std/print.cpoint
import @std/file.cpoint
func main(){
    var fd : int = openfile("test_io.txt")
    printstr("fd : ")
    printi(fd)
    var out : int = writefile(fd, "test hello world\n")
    printstr("write output")
    printi(out)
    closefile(fd)
}