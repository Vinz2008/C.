import @std/print.cpoint

extern printf(format : i8 ptr ...) i32;

func main() void {
  var i : i32 = 2
  var temp : i32 = 3
  var p : i32 ptr = addr i
  p = addr temp
  printi(i)
  // printi(deref p)
  var s : i8 ptr = "hello world"
  var c : i8 = s[2]
  // printf("%c\n", c)
  var b : bool
}