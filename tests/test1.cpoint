import @std/print.cpoint
include @std/libc.hpoint

func main(){
  var i : int = 2
  var temp : int = 3
  var p : int ptr = addr i
  p = temp
  printi(i)
  var s : i8 ptr = "hello world"
  var c : i8 = s[2]
  printf("%c", c)
  var b : bool
}