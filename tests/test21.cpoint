import @std/print.cpoint
import @std/pair.cpoint

func main() double {
    var a : double = 2
    if a == 2 {
        printi(argc)
    }
    var t : struct pair~int~ 
    t.first = 32
    printi(t.get_first(a))
    0
}