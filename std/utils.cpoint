import expect.cpoint
import print.cpoint

mod std {
mod utils {
func average(x, y) int {
  (x + y) / 2
}

func absolute(v) int {
  if v < 0 { 
    0 - v 
  } else {
   return v
  }
}
}

struct utilstest {
  var a : i8
  var b : i8
}
}

test "testing average" {
  expect(std::utils::average(2, 4) == 3)
  expect(std::utils::average(10, 20) == 15)
}

test "testing absolute" {
  expect(std::utils::absolute(-10) == 10)
}