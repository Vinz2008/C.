import print.cpoint

func handle_args(argc : i32, argv : i8 ptr ptr) void {
    printi(argc)
    printstr(argv[0])
    for i = 0, i < argc-1, 1.0 {
        printstr(argv[i])
        printi(i)
    }
}
